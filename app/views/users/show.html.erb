<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<div class="row">
  <div class="col-md-8">
      <% if @user.tasks.any? %>
      <h3>招募(<%=@user.tasks.count %></h3>
      <ol class="tasks">
        <%=render @tasks %>
      </ol>
        <%= will_paginate @tasks %>
    <%end %>
  </div>
</div>